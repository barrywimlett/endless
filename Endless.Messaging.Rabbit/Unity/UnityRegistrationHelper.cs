using Endless.Messaging.Rabbit.Interfaces;

namespace Endless.Messaging.Rabbit.Unity
{
    /*
    public static class UnityRegistrationHelper
    {
        public static void RegisterRabbitDefaultConnectionSettings(this IUnityContainer container)
        {
            var messagingSettings = RabbitConnectionManager.Singleton.GetDefaultConnectionSetting();
            container.RegisterInstance<IRabbitMessagingSettings>(messagingSettings);
        }
    }
    */
}
